<?xml version="1.0" encoding="utf-8"?>

<!-- Review Markup For This Page [√√√√] -->

<LinearLayout xmlns:tools="http://schemas.android.com/tools"
    android:background="#000000"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:orientation="vertical"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- layout_weight=1 is meant to make sure each one takes up an equal amount of vertical screenspace. -->
    <!-- layout_weight=1 is meant to make sure each one takes up an equal amount of vertical screenspace. -->
    <!-- layout_weight=1 is meant to make sure each one takes up an equal amount of vertical screenspace. -->

    <TextView
        android:fontFamily="sans-serif-black"
        android:gravity="center"
        android:id="@+id/mxSalientWarning1"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_width="fill_parent"
        android:text="@string/mtSalientSuggestion1"
        android:textSize="34dp"
        android:textAppearance="@android:style/TextAppearance.Material.Display1"
        android:textColor="#FF0000"
        tools:ignore="SpUsage" />

    <TextView
        android:fontFamily="sans-serif-black"
        android:gravity="center"
        android:id="@+id/mxSalientWarning2"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_width="match_parent"
        android:text="@string/mtSalientSuggestion2"
        android:textSize="34dp"
        android:textAppearance="@android:style/TextAppearance.Material.Display1"
        android:textColor="#FFFFFF"
        tools:ignore="SpUsage" />
</LinearLayout>